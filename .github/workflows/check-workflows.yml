# Checks the n8n workflow exports in automation/n8n against the invariants in
# AGENTS.md. Every check exists for a defect that reached main at least once.
# The script is stdlib-only Python, so there is nothing to install and it never
# touches the live n8n instance.
name: check workflow exports

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: '3.12'

      - name: Check the workflow exports
        run: python3 automation/scripts/check-workflows.py
